library(EIAapi)
library(tidyverse)
library(ggplot2)
library(httr)
library(jsonlite)
api_key  <- "XH6BAfw0jn83wuor37ggwPEA98TEDhX58hDlBMWD"
api_path <- "electricity/retail-sales/data/"
install.packages("EIAapi")
install.packages("tidyverse")
install.packages("ggplot2")
library(EIAapi)
library(tidyverse)
library(ggplot2)
library(httr)
library(jsonlite
api_key  <- "XH6BAfw0jn83wuor37ggwPEA98TEDhX58hDlBMWD"
api_key  <- "XH6BAfw0jn83wuor37ggwPEA98TEDhX58hDlBMWD"
api_path <- "electricity/retail-sales/data/"
# Setting up the frequency and dates - between nov 2021 and february 2026
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=value",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=COM&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_com <- json_content$response$data
print(head(df_ep_com ))
# Saving as csv
write.csv(df_ep_com, file = "df_ep_com.csv")
api_key  <- "XH6BAfw0jn83wuor37ggwPEA98TEDhX58hDlBMWD"
api_path <- "electricity/retail-sales/data/"
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=value",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=COM&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_com <- json_content$response$data
print(head(df_ep_com ))
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=COM&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_com <- json_content$response$data
print(head(df_ep_com ))
# Saving as csv
write.csv(df_ep_com, file = "df_ep_com.csv")
View(df_ep_com)
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=IND&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_com <- json_content$response$data
print(head(df_ep_ind ))
# Setting up the frequency and dates - between nov 2021 and february 2026
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=IND&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
(df_ep_ind <- json_content$response$data
print(head(df_ep_ind ))
# Setting up the frequency and dates - between nov 2021 and february 2026
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=IND&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
(df_ep_ind <- json_content$response$data
print(head(df_ep_ind))
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=IND&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
(df_ep_ind <- json_content$response$data
print(head(df_ep_ind))
# Setting up the frequency and dates - between nov 2021 and february 2026
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=IND&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_ind <- json_content$response$data
print(head(df_ep_ind))
# Saving as csv
write.csv(df_ep_ind, file = "df_ep_ind.csv")
View(df_ep_ind)
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=COM&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_com <- json_content$response$data
print(head(df_ep_com ))
# Saving as csv
write.csv(df_ep_com, file = "df_ep_com.csv")
View(df_ep_com)
# Setting up the frequency and dates - between nov 2021 and february 2026
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=RES&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_res <- json_content$response$data
print(head(df_ep_res))
# Saving as csv
write.csv(df_ep_ind, file = "df_ep_res.csv")
View(df_ep_res)
# Setting up the frequency and dates - between nov 2021 and february 2026
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=TRA&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_tra <- json_content$response$data
print(head(df_ep_res))
# Saving as csv
write.csv(df_ep_tra, file = "df_ep_tra.csv")
# Setting up the frequency and dates - between nov 2021 and february 2026
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=OTH&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_tra <- json_content$response$data
print(head(df_ep_oth))
# Setting up the frequency and dates - between nov 2021 and february 2026
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=price",
"&start=", start_date,
"&end=", end_date,
"&facets[sectorid][]=OTH&",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000")
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ep_oth <- json_content$response$data
print(head(df_ep_oth))
# Saving as csv
write.csv(df_ep_oth, file = "df_ep_oth.csv")
View(df_ep_oth)
install.packages("EIAapi")
install.packages("tidyverse")
install.packages("ggplot2")
library(EIAapi)
library(tidyverse)
library(ggplot2)
library(httr)
library(jsonlite)
api_key  <- "XH6BAfw0jn83wuor37ggwPEA98TEDhX58hDlBMWD"
api_path <- "gas/cons/sum/data/"
api_key  <- "XH6BAfw0jn83wuor37ggwPEA98TEDhX58hDlBMWD"
api_path <- "gas/cons/sum/data/"
frequency <- "monthly"
start_date <- "2023-07"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=value",
"&start=", start_date,
"&end=", end_date,
"&facets[process][]=VGT&facets[process][]=VIN&facets[process][]=VRS",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000"
)
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ng_demand_1 <- json_content$response$data
print(head(df_ng_1 ))
# Setting up the frequency and dates - between july 2023 and february 2026
frequency <- "monthly"
start_date <- "2023-07"
end_date   <- "2026-02"
# URL for the api, we need to include the api key here, maximum length for each query is 5000
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly&data[0]=value",
"&start=", start_date,
"&end=", end_date,
"&facets[process][]=VGT&facets[process][]=VIN&facets[process][]=VRS",
"&sort[0][column]=period&sort[0][direction]=desc&length=5000"
)
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled from - July 2023 - Feb 2026
df_ng_demand_1 <- json_content$response$data
print(head(df_ng_demand_1 ))
# Saving as csv
write.csv(df_ng_demand_1, file = "df_ng_demand_1.csv")
# Setting up the frequency and dates - between july 2023 and february 2026
frequency <- "monthly"
start_date <- "2023-07"
end_date   <- "2026-02"
# 2. Separated Date Variables (July 2023 to Feb 2026)
start_val <- "2023-07"
end_val   <- "2026-02"
# 3. Build the URL
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly",
"&data[0]=value",
"&facets[process][]=VGT", # Delivered to Consumers
"&facets[process][]=VIN", # Industrial
"&facets[process][]=VRS", # Residential
"&start=", start_val,
"&end=", end_val,
"&sort[0][column]=period",
"&sort[0][direction]=desc",
"&offset=0",
"&length=5000"
)
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled - November 2021 to June 2023
df_ng_demand_1 <- json_content$response$data
print(head(df_ng_demand_1 ))
# Saving as csv
write.csv(df_ng_demand_1, file = "df_ng_demand_1.csv
# Setting up the frequency and dates - between july 2023 and february 2026
frequency <- "monthly"
# Setting up the frequency and dates - between july 2023 and february 2026
frequency <- "monthly"
start_date <- "2023-07"
end_date   <- "2026-02"
# 2. Separated Date Variables (July 2023 to Feb 2026)
start_val <- "2023-07"
end_val   <- "2026-02"
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly",
"&data[0]=value",
"&facets[process][]=VGT", # Delivered to Consumers
"&facets[process][]=VIN", # Industrial
"&facets[process][]=VRS", # Residential
"&start=", start_val,
"&end=", end_val,
"&sort[0][column]=period",
"&sort[0][direction]=desc",
"&offset=0",
"&length=5000"
)
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled - November 2021 to June 2023
df_ng_demand_1 <- json_content$response$data
print(head(df_ng_demand_1 ))
# Saving as csv
write.csv(df_ng_demand_1, file = "df_ng_demand_1.csv")
install.packages("EIAapi")
install.packages("tidyverse")
install.packages("ggplot2")
library(EIAapi)
library(tidyverse)
library(ggplot2)
library(httr)
library(jsonlite)
api_key  <- "XH6BAfw0jn83wuor37ggwPEA98TEDhX58hDlBMWD"
api_path <- "gas/cons/sum/data/"
# Setting up the frequency and dates - between july 2023 and february 2026
frequency <- "monthly"
start_date <- "2023-07"
end_date   <- "2026-02"
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly",
"&data[0]=value",
"&facets[process][]=VGT", # Delivered to Consumers
"&facets[process][]=VIN", # Industrial
"&facets[process][]=VRS", # Residential
"&start=", start_date,
"&end=", end_date,
"&sort[0][column]=period",
"&sort[0][direction]=desc",
"&offset=0",
"&length=5000"
)
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled - November 2021 to June 2023
df_ng_demand_1 <- json_content$response$data
print(head(df_ng_demand_1 ))
# Saving as csv
write.csv(df_ng_demand_1, file = "df_ng_demand_1.csv")
View(response)
api_key  <- "XH6BAfw0jn83wuor37ggwPEA98TEDhX58hDlBMWD"
api_path <- "/natural-gas/cons/sum/data/"
# Setting up the frequency and dates - between july 2023 and february 2026
frequency <- "monthly"
start_date <- "2021-11"
end_date   <- "2023-06"
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly",
"&data[0]=value",
"&facets[process][]=VGT", # Delivered to Consumers
"&facets[process][]=VIN", # Industrial
"&facets[process][]=VRS", # Residential
"&start=", start_date,
"&end=", end_date,
"&sort[0][column]=period",
"&sort[0][direction]=desc",
"&offset=0",
"&length=5000"
)
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled - November 2021 to June 2023
df_ng_demand_1 <- json_content$response$data
print(head(df_ng_demand_1 ))
# Saving as csv
write.csv(df_ng_demand_1, file = "df_ng_demand_1.csv")
View(df_ng_demand_1)
# Setting up the frequency and dates - between july 2023 and february 2026
frequency <- "monthly"
start_date <- "2023-07"
end_date   <- "2026-02"
url <- paste0(
"https://api.eia.gov/v2/", api_path,
"/?api_key=", api_key,
"&frequency=monthly",
"&data[0]=value",
"&facets[process][]=VGT", # Delivered to Consumers
"&facets[process][]=VIN", # Industrial
"&facets[process][]=VRS", # Residential
"&start=", start_date,
"&end=", end_date,
"&sort[0][column]=period",
"&sort[0][direction]=desc",
"&offset=0",
"&length=5000"
)
#Pulling the response using get
response <- GET(url)
#Parse the JSON content
json_content <- fromJSON(content(response, as = "text", encoding = "UTF-8"))
#Extract the data frame, since the dataframe is pulled - November 2021 to June 2023
df_ng_demand_2 <- json_content$response$data
print(head(df_ng_demand_2 ))
# Saving as csv
write.csv(df_ng_demand_2, file = "df_ng_demand_2.csv")
